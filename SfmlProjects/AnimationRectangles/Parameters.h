#pragma once

const sf::Time TICK = sf::seconds(1.f / 60.f);

const int RECTANGLES_NUMBER = 3;
const int RECTANGLES_SIDE_LONG = 20;
const int DISTANCE_BETWEEN = 20;
const sf::Color RECTANGLE_COLOR = sf::Color::Red;